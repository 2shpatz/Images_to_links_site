# wrangler.toml
name = "images-to-links-site"
compatibility_date = "2024-12-01"
pages_build_output_dir = "site"

# KV binding for SITE_DATA is configured via Cloudflare Dashboard:
#   Workers & Pages → images-to-links-site → Settings → Bindings → Add → KV
#   Variable name: SITE_DATA
